{% extends 'base.html.twig' %}

{% block title %}Profile{% endblock %}

{% block body %}
    <h1>Profile</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ profile.id }}</td>
            </tr>
            <tr>
                <th>Fullname</th>
                <td>{{ profile.fullname }}</td>
            </tr>
            <tr>
                <th>AccountNumber</th>
                <td>{{ profile.AccountNumber }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ profile.country }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ profile.address }}</td>
            </tr>
            <tr>
                <th>Iban</th>
                <td>{{ profile.iban }}</td>
            </tr>
            <tr>
                <th>Swift</th>
                <td>{{ profile.swift }}</td>
            </tr>
            <tr>
                <th>Balance</th>
                <td>{{ profile.balance }}</td>
            </tr>
            <tr>
                <th>Currency</th>
                <td>{{ profile.currency }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ profile.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_index') }}">back to list</a>

    <a href="{{ path('user_edit', {'id': profile.id}) }}">edit</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
